# HousekeepingOps (legacy)

Housekeeping HTTP routes now live in **SecurityOps.Api** under `Controllers/Housekeeping/`.

Use a **single** Render deployment (`backend/SecurityOps/Dockerfile`). This project is kept only for optional local split testing on port 5116.
